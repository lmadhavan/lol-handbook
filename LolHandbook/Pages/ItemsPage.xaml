<Page
    x:Class="LolHandbook.Pages.ItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:stubs="using:LolHandbook.ViewModels.Stubs"
    xmlns:resources="using:LolHandbook.Resources"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=stubs:StubItemsViewModel, IsDesignTimeCreatable=True}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <resources:ItemDataTemplate />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="12,4,12,8" Grid.Row="0">
            <TextBlock Text="Category:" VerticalAlignment="Center" />

            <Button Style="{StaticResource TextBlockButtonStyle}" Content="{Binding TagFilter}">
                <Button.Flyout>
                    <Flyout x:Name="TagFlyout">
                        <ListBox x:Name="TagList" ItemsSource="{Binding Tags}" SelectionChanged="TagList_SelectionChanged" />
                    </Flyout>
                </Button.Flyout>
            </Button>
        </StackPanel>

        <ProgressBar IsIndeterminate="True" Visibility="{Binding Loading, Converter={StaticResource VisibilityConverter}}" Grid.Row="1" />

        <GridView x:Name="ItemsView" SelectionMode="None" ItemsSource="{Binding Items}" ItemTemplate="{StaticResource ItemDataTemplate}" IsItemClickEnabled="True" ItemClick="GridView_ItemClick" Grid.Row="2" />
    </Grid>
</Page>
