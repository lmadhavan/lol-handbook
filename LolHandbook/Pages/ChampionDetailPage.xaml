<Page
    x:Class="LolHandbook.Pages.ChampionDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dd="using:DataDragon"
    xmlns:stubs="using:LolHandbook.ViewModels.Stubs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=stubs:StubChampionDetailViewModel, IsDesignTimeCreatable=True}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            
            <!-- Champion bio -->
            
            <Grid Background="{ThemeResource PivotNavButtonBackgroundThemeBrush}" Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="96" Height="96" Grid.Column="0">
                    <Image Source="{Binding ChampionBase.ImageUri}" Stretch="UniformToFill" />
                </Border>

                <StackPanel Orientation="Vertical" Margin="12,0" Grid.Column="1">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{Binding ChampionBase.Name}" />
                    <TextBlock Text="{Binding ChampionBase.Title}" />
                    <TextBlock FontWeight="Medium" Margin="0,12,0,0" Text="{Binding ChampionBase.Tags, Converter={StaticResource TagsConverter}}" />
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,12">
                    <TextBlock Text="{Binding ChampionBase.Blurb, Converter={StaticResource HtmlConverter}}" TextWrapping="Wrap" />

                    <Button Style="{StaticResource TextBlockButtonStyle}" Padding="0" FontWeight="Medium" Content="More">
                        <Button.Flyout>
                            <Flyout>
                                <Flyout.FlyoutPresenterStyle>
                                    <Style TargetType="FlyoutPresenter">
                                        <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
                                        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                                        <Setter Property="IsTabStop" Value="True" />
                                        <Setter Property="TabNavigation" Value="Cycle" />
                                    </Style>
                                </Flyout.FlyoutPresenterStyle>
                                <TextBlock Text="{Binding ChampionDetail.Lore, Converter={StaticResource HtmlConverter}}" TextWrapping="Wrap" />
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Vertical" Margin="20">
                
                <!-- Abilities -->
                
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Abilities" />

                <Grid Margin="0,16" DataContext="{Binding ChampionDetail.Passive}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="64" Height="64" Grid.Column="0">
                        <Image Source="{Binding ImageUri}" Stretch="UniformToFill" />
                    </Border>

                    <StackPanel Orientation="Vertical" Margin="12,0" Grid.Column="1">
                        <TextBlock FontWeight="SemiLight" Style="{StaticResource SubtitleTextBlockStyle}" Text="{Binding Name}" />
                        <TextBlock FontWeight="Medium" Text="Passive" />
                        <TextBlock />
                    </StackPanel>

                    <TextBlock Margin="0,12,0,0" Text="{Binding Description}" TextWrapping="Wrap" Grid.Row="1" Grid.ColumnSpan="2" />
                </Grid>

                <ListView SelectionMode="None" IsItemClickEnabled="False" IsHitTestVisible="False" ItemsSource="{Binding ChampionDetail.Spells}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="dd:ChampionSpell">
                            <Grid Margin="0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="64" Height="64" Grid.Column="0">
                                    <Image Source="{x:Bind ImageUri, Converter={StaticResource ImageUriConverter}}" Stretch="UniformToFill" x:Phase="1" />
                                </Border>

                                <StackPanel Orientation="Vertical" Margin="12,0" Grid.Column="1">
                                    <TextBlock FontWeight="SemiLight" Style="{StaticResource SubtitleTextBlockStyle}" Text="{x:Bind Name}" />
                                    <TextBlock FontWeight="Medium" Text="{x:Bind CooldownBurn, Converter={StaticResource CooldownConverter}}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <TextBlock Margin="0,12,0,0" Text="{x:Bind Description}" TextWrapping="Wrap" Grid.Row="1" Grid.ColumnSpan="2" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
